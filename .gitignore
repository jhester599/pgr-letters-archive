# Python
__pycache__/
*.py[cod]
*.pyo
.env
.venv/
venv/
*.egg-info/
dist/
build/

# Raw audio (temporary)
data/audio_raw/*.mp3
data/audio_raw/*.wav
data/audio_raw/*.m4a
data/audio_raw/*.aac

# Published audio is hosted from the GitHub Releases audio-library release.
docs/audio/*.mp3
docs/audio_tts/*.mp3

# NotebookLM Playwright browser session state
.browser_state/
playwright-report/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
.worktrees/
# Ignore Claude Code worktree/session artifacts, but keep the shared project
# settings file — it is deliberately committed and was already tracked before
# this rule was added, so without the negation the two contradict each other.
.claude/
!.claude/settings.json
